# Cloud Run backend URL (no trailing slash)
# After deploying the backend, get the URL from: gcloud run services describe synthetic-student-generator --region us-central1 --format="value(status.url)"
API_TARGET=https://your-service-hash.us-central1.run.app
